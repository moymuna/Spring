spring.application.name=HRM_system

spring.datasource.driver-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/hrm
spring.datasource.username=root
spring.datasource.password=1234

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

server.port=8085

# Browser origins allowed to call this API (the Angular dev server).
app.cors.allowed-origins=http://localhost:4200,http://localhost:4300,http://192.168.88.250:4200


#Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jahansadia
spring.mail.password=rnxz vntf xlid tqss
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000



#Image Configuration

# Relative to the JVM's working directory when the app starts â€” portable across machines,
# unlike the previous hardcoded drive-specific path. Override via env var if needed.
image.upload.dir=./assets
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


jwt.secret=7f3k9mX2pQrL8nVwYdA5sEuJhBcGiTemran6lKvF1yWqPmHbCxDjZ0eUsIgOt
# Access token is short-lived now that refresh tokens exist to renew it silently.
jwt.expiration=1800000
# Refresh token (the only auth token persisted in the DB) â€” 7 days.
jwt.refresh-expiration=604800000


jwt.verification-expiration=3600000
jwt.reset-expiration=900000

# Base URL of the Angular frontend, used to build links in verification/reset emails
app.frontend-url=http://localhost:4200